spring.datasource.initialization-mode=always
spring.datasource.platform=postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/bankservice
spring.datasource.username=postgres
spring.datasource.password=054Mthokozisi

#this is the dialect that is used
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

#show queries
spring.jpa.show-sql=true

management.security.enabled =false

#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=update
bank.minimumDeposit = 1000.00
bank.minimumBalance = 1000.00
bank.overDraftBalance = -100000.00
bank.deposit =0.00
bank.withdraw = 0.00